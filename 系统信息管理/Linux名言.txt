安装系统最小化：选择最小化，简化的Linux系统，无用的包不装；无用的软件不装；
开机启动最小化：无用的服务不开启；
操作命令最小化：达到目的即可,参数尽可能的少，”rm –f text.txt”不要用”rm –rf text.txt”
登录用户最小化：平时使用普通用户登录，一般不用root登录
普通用户授权最小化： 只给必须的使用系统的命令；其他多余的命令都不给予
Linux系统及目录的权限最小化： 禁止随意的在系统目录下创建，修改，删除文件；能只用读的情况不要给予写的权限；
